# Local-only agent tooling that is gitignored (see .gitignore) but needed
# in every worktree — git worktree only checks out tracked files, so these
# paths must be copied/symlinked into a new worktree manually.
.agents/
.claude/
.codex/
.superpowers/
skills-lock.json
